// the actual logic should be SSR
// we should just return a JSON stringified array of values
//
// depending on a boolean prop named `animation` we will either:
// - send back the fully completed room
// - send back a single step and wait for the step to be requested
export default 'ROOM TYPE!!!'